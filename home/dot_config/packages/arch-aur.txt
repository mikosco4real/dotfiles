# AUR packages, installed with paru by
# .chezmoiscripts/run_onchange_before_11-packages-linux.sh.tmpl
#
# Only tools with NO package in core/extra belong here. Everything else goes in
# arch.txt — the AUR builds from source and is not covered by Arch's own
# testing, so the smaller this file is, the better.
#
# Skipped entirely when paru is missing, or under CI (makepkg refuses to run as
# root, and the container has nothing to build for). A missing AUR package is a
# warning, never a failed bootstrap.

# tmux status-left segment, enabled by @status_gitmux in tmux.conf.
gitmux

# Release tooling.
goreleaser
openapi-generator

# Fetch/system-info toy used by the shell greeting.
nerdfetch
